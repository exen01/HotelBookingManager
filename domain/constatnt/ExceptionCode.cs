namespace HotelBookingManager.domain.constatnt
{
    enum ExceptionCode
    {
        WRONG_CLIENT_NAME,
        WRONG_CLIENT_ADDRESS,
        WRONG_CLIENT_EMAIL,
        WRONG_CLIENT_PHONE,
        INVALID_CLIENT,
        INVALID_ARRIVAL_DATE,
        INVALID_DEPARTURE_DATE,
        INVALID_ROOM
    }
}
